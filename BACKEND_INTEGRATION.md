# Backend Integration Guide

This guide shows how to integrate AWS services into your Abjad React application.

---

## 1. Configure Amplify in Your App

### File: `src/aws-config.js` (Create this file)

```javascript
import { Amplify } from 'aws-amplify';

const awsConfig = {
  Auth: {
    Cognito: {
      userPoolId: 'us-east-1_XXXXXXXXX', // Will be auto-generated by amplify init
      userPoolClientId: 'XXXXXXXXXXXXXXXXXXXXXXXXXX', // Will be auto-generated
      identityPoolId: 'us-east-1:XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX', // Optional
      loginWith: {
        email: true,
      },
      signUpVerificationMethod: 'code',
      userAttributes: {
        email: {
          required: true,
        },
        name: {
          required: true,
        },
      },
      passwordFormat: {
        minLength: 8,
        requireLowercase: true,
        requireUppercase: true,
        requireNumbers: true,
        requireSpecialCharacters: true,
      },
    },
  },
  API: {
    GraphQL: {
      endpoint: 'https://XXXXXXXXXXXX.appsync-api.us-east-1.amazonaws.com/graphql',
      region: 'us-east-1',
      defaultAuthMode: 'userPool',
    },
  },
  Storage: {
    S3: {
      bucket: 'abjad-receipts-images-XXXXX',
      region: 'us-east-1',
    },
  },
};

Amplify.configure(awsConfig);

export default awsConfig;
```

**Note**: After running `amplify push`, these values will be in `src/aws-exports.js`. You can import that instead:

```javascript
import { Amplify } from 'aws-amplify';
import awsExports from './aws-exports';

Amplify.configure(awsExports);
```

---

## 2. Authentication Components

### File: `src/components/Auth/LoginForm.jsx`

```javascript
import React, { useState } from 'react';
import { signIn, signOut, getCurrentUser } from 'aws-amplify/auth';
import { motion } from 'framer-motion';
import { Mail, Lock, Eye, EyeOff } from 'lucide-react';

const COLORS = {
  growth: '#005143',
  innovation: '#41E661',
  clarity: '#FEF5E8',
  depth: '#121B22'
};

export function LoginForm({ onSuccess }) {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [showPassword, setShowPassword] = useState(false);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const handleLogin = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError('');

    try {
      const { isSignedIn, nextStep } = await signIn({
        username: email,
        password: password,
      });

      if (isSignedIn) {
        const user = await getCurrentUser();
        onSuccess(user);
      }
    } catch (err) {
      console.error('Login error:', err);
      setError(err.message || 'Failed to login. Please check your credentials.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="bg-white rounded-3xl p-8 shadow-2xl max-w-md w-full mx-4"
    >
      <div className="text-center mb-8">
        <h2 className="text-3xl font-bold mb-2" style={{ color: COLORS.depth }}>
          Welcome to Abjad
        </h2>
        <p className="text-gray-600" dir="rtl">أبجديات إدارة المنزل</p>
      </div>

      {error && (
        <div className="mb-4 p-4 bg-red-50 border-2 border-red-200 rounded-2xl">
          <p className="text-red-600 text-sm font-semibold">{error}</p>
        </div>
      )}

      <form onSubmit={handleLogin} className="space-y-4">
        <div>
          <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
            Email
          </label>
          <div className="relative">
            <Mail className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
            <input
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              placeholder="fulan.alfulani@example.com"
              className="w-full pl-12 pr-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
              required
            />
          </div>
        </div>

        <div>
          <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
            Password
          </label>
          <div className="relative">
            <Lock className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
            <input
              type={showPassword ? 'text' : 'password'}
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              placeholder="••••••••"
              className="w-full pl-12 pr-12 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
              required
            />
            <button
              type="button"
              onClick={() => setShowPassword(!showPassword)}
              className="absolute right-4 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-gray-600"
            >
              {showPassword ? <EyeOff className="w-5 h-5" /> : <Eye className="w-5 h-5" />}
            </button>
          </div>
        </div>

        <button
          type="submit"
          disabled={loading}
          className="w-full py-4 rounded-2xl font-bold text-white transition-all active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed"
          style={{ backgroundColor: COLORS.growth }}
        >
          {loading ? 'Signing in...' : 'Sign In'}
        </button>
      </form>

      <div className="mt-6 text-center">
        <button
          className="text-sm font-semibold hover:underline"
          style={{ color: COLORS.growth }}
        >
          Forgot password?
        </button>
      </div>
    </motion.div>
  );
}

export async function handleLogout() {
  try {
    await signOut();
    window.location.href = '/';
  } catch (error) {
    console.error('Logout error:', error);
  }
}
```

### File: `src/components/Auth/SignUpForm.jsx`

```javascript
import React, { useState } from 'react';
import { signUp, confirmSignUp } from 'aws-amplify/auth';
import { motion, AnimatePresence } from 'framer-motion';
import { Mail, Lock, User, Phone } from 'lucide-react';

const COLORS = {
  growth: '#005143',
  innovation: '#41E661',
  clarity: '#FEF5E8',
  depth: '#121B22'
};

export function SignUpForm({ onSuccess }) {
  const [step, setStep] = useState(1); // 1: signup, 2: confirmation
  const [formData, setFormData] = useState({
    email: '',
    password: '',
    name: '',
    phone: '',
  });
  const [confirmationCode, setConfirmationCode] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const handleSignUp = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError('');

    try {
      const { isSignUpComplete, userId, nextStep } = await signUp({
        username: formData.email,
        password: formData.password,
        options: {
          userAttributes: {
            email: formData.email,
            name: formData.name,
            phone_number: formData.phone,
          },
        },
      });

      if (nextStep.signUpStep === 'CONFIRM_SIGN_UP') {
        setStep(2);
      }
    } catch (err) {
      console.error('Sign up error:', err);
      setError(err.message || 'Failed to create account');
    } finally {
      setLoading(false);
    }
  };

  const handleConfirmSignUp = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError('');

    try {
      const { isSignUpComplete } = await confirmSignUp({
        username: formData.email,
        confirmationCode: confirmationCode,
      });

      if (isSignUpComplete) {
        onSuccess();
      }
    } catch (err) {
      console.error('Confirmation error:', err);
      setError(err.message || 'Invalid confirmation code');
    } finally {
      setLoading(false);
    }
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="bg-white rounded-3xl p-8 shadow-2xl max-w-md w-full mx-4"
    >
      <AnimatePresence mode="wait">
        {step === 1 ? (
          <motion.div key="signup" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}>
            <div className="text-center mb-8">
              <h2 className="text-3xl font-bold mb-2" style={{ color: COLORS.depth }}>
                Create Account
              </h2>
              <p className="text-gray-600">Join Abjad today</p>
            </div>

            {error && (
              <div className="mb-4 p-4 bg-red-50 border-2 border-red-200 rounded-2xl">
                <p className="text-red-600 text-sm font-semibold">{error}</p>
              </div>
            )}

            <form onSubmit={handleSignUp} className="space-y-4">
              <div>
                <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
                  Full Name
                </label>
                <div className="relative">
                  <User className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type="text"
                    value={formData.name}
                    onChange={(e) => setFormData({ ...formData, name: e.target.value })}
                    placeholder="Fulan AlFulani"
                    className="w-full pl-12 pr-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
                    required
                  />
                </div>
              </div>

              <div>
                <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
                  Email
                </label>
                <div className="relative">
                  <Mail className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type="email"
                    value={formData.email}
                    onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                    placeholder="fulan@example.com"
                    className="w-full pl-12 pr-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
                    required
                  />
                </div>
              </div>

              <div>
                <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
                  Phone
                </label>
                <div className="relative">
                  <Phone className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type="tel"
                    value={formData.phone}
                    onChange={(e) => setFormData({ ...formData, phone: e.target.value })}
                    placeholder="+966501234567"
                    className="w-full pl-12 pr-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
                    required
                  />
                </div>
              </div>

              <div>
                <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
                  Password
                </label>
                <div className="relative">
                  <Lock className="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type="password"
                    value={formData.password}
                    onChange={(e) => setFormData({ ...formData, password: e.target.value })}
                    placeholder="Min 8 characters"
                    className="w-full pl-12 pr-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0"
                    required
                    minLength={8}
                  />
                </div>
                <p className="text-xs text-gray-500 mt-1">
                  Must include uppercase, lowercase, number, and special character
                </p>
              </div>

              <button
                type="submit"
                disabled={loading}
                className="w-full py-4 rounded-2xl font-bold text-white transition-all active:scale-95 disabled:opacity-50"
                style={{ backgroundColor: COLORS.growth }}
              >
                {loading ? 'Creating account...' : 'Sign Up'}
              </button>
            </form>
          </motion.div>
        ) : (
          <motion.div key="confirm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}>
            <div className="text-center mb-8">
              <h2 className="text-3xl font-bold mb-2" style={{ color: COLORS.depth }}>
                Verify Email
              </h2>
              <p className="text-gray-600">
                We sent a code to {formData.email}
              </p>
            </div>

            {error && (
              <div className="mb-4 p-4 bg-red-50 border-2 border-red-200 rounded-2xl">
                <p className="text-red-600 text-sm font-semibold">{error}</p>
              </div>
            )}

            <form onSubmit={handleConfirmSignUp} className="space-y-4">
              <div>
                <label className="block text-sm font-semibold mb-2" style={{ color: COLORS.depth }}>
                  Confirmation Code
                </label>
                <input
                  type="text"
                  value={confirmationCode}
                  onChange={(e) => setConfirmationCode(e.target.value)}
                  placeholder="123456"
                  className="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-green-500 focus:ring-0 text-center text-2xl tracking-widest"
                  required
                  maxLength={6}
                />
              </div>

              <button
                type="submit"
                disabled={loading}
                className="w-full py-4 rounded-2xl font-bold text-white transition-all active:scale-95 disabled:opacity-50"
                style={{ backgroundColor: COLORS.growth }}
              >
                {loading ? 'Verifying...' : 'Verify & Continue'}
              </button>
            </form>
          </motion.div>
        )}
      </AnimatePresence>
    </motion.div>
  );
}
```

---

## 3. API Integration with GraphQL

### File: `src/api/graphql/schema.graphql`

```graphql
type User @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  email: String!
  name: String!
  nameAr: String
  phone: String
  role: String!
  avatar: String
  canViewBills: Boolean
  canManageMembers: Boolean
  rooms: [Room] @hasMany(indexName: "byUser", fields: ["id"])
  tasks: [Task] @hasMany(indexName: "byAssignee", fields: ["id"])
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type Room @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  userId: ID! @index(name: "byUser")
  name: String!
  x: Float!
  y: Float!
  width: Float!
  height: Float!
  status: String!
  tasks: [Task] @hasMany(indexName: "byRoom", fields: ["id"])
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type Task @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  roomId: ID! @index(name: "byRoom")
  assignedTo: ID! @index(name: "byAssignee")
  title: String!
  type: String!
  status: String!
  priority: String!
  estimatedTime: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type HouseholdTask @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  userId: ID! @index(name: "byUser")
  title: String!
  titleAr: String
  type: String!
  assignedTo: ID!
  dueDate: AWSDate!
  amount: Float
  priority: String!
  status: String!
  provider: String
  notes: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type Ticket @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  userId: ID! @index(name: "byUser")
  fromUser: ID!
  title: String!
  description: String!
  status: String!
  priority: String!
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type Asset @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  userId: ID! @index(name: "byUser")
  name: String!
  nameAr: String
  category: String!
  location: String!
  purchaseDate: AWSDate
  warranty: String
  value: Float
  receiptUrl: String
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}

type Booking @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  userId: ID! @index(name: "byUser")
  serviceId: String!
  serviceName: String!
  date: AWSDate!
  time: String!
  price: Float!
  status: String!
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
}
```

---

## 4. API Service Layer

### File: `src/services/api.js`

```javascript
import { generateClient } from 'aws-amplify/api';
import { getCurrentUser } from 'aws-amplify/auth';

const client = generateClient();

// Queries
export const listRooms = /* GraphQL */ `
  query ListRooms($filter: ModelRoomFilterInput, $limit: Int) {
    listRooms(filter: $filter, limit: $limit) {
      items {
        id
        name
        x
        y
        width
        height
        status
        tasks {
          items {
            id
            title
            type
            status
            priority
            assignedTo
            estimatedTime
          }
        }
        createdAt
        updatedAt
      }
    }
  }
`;

export const listTasks = /* GraphQL */ `
  query ListTasks($filter: ModelTaskFilterInput) {
    listTasks(filter: $filter) {
      items {
        id
        roomId
        title
        type
        status
        priority
        assignedTo
        estimatedTime
        createdAt
        updatedAt
      }
    }
  }
`;

// Mutations
export const createRoom = /* GraphQL */ `
  mutation CreateRoom($input: CreateRoomInput!) {
    createRoom(input: $input) {
      id
      name
      x
      y
      width
      height
      status
      createdAt
      updatedAt
    }
  }
`;

export const createTask = /* GraphQL */ `
  mutation CreateTask($input: CreateTaskInput!) {
    createTask(input: $input) {
      id
      roomId
      title
      type
      status
      priority
      assignedTo
      estimatedTime
      createdAt
      updatedAt
    }
  }
`;

export const updateTask = /* GraphQL */ `
  mutation UpdateTask($input: UpdateTaskInput!) {
    updateTask(input: $input) {
      id
      status
      updatedAt
    }
  }
`;

// API Functions
export async function fetchRooms() {
  try {
    const result = await client.graphql({
      query: listRooms,
    });
    return result.data.listRooms.items;
  } catch (error) {
    console.error('Error fetching rooms:', error);
    throw error;
  }
}

export async function fetchUserTasks(userId) {
  try {
    const result = await client.graphql({
      query: listTasks,
      variables: {
        filter: {
          assignedTo: { eq: userId }
        }
      }
    });
    return result.data.listTasks.items;
  } catch (error) {
    console.error('Error fetching tasks:', error);
    throw error;
  }
}

export async function addTask(taskData) {
  try {
    const result = await client.graphql({
      query: createTask,
      variables: {
        input: taskData
      }
    });
    return result.data.createTask;
  } catch (error) {
    console.error('Error creating task:', error);
    throw error;
  }
}

export async function completeTask(taskId) {
  try {
    const result = await client.graphql({
      query: updateTask,
      variables: {
        input: {
          id: taskId,
          status: 'completed'
        }
      }
    });
    return result.data.updateTask;
  } catch (error) {
    console.error('Error updating task:', error);
    throw error;
  }
}
```

---

## 5. File Upload to S3

### File: `src/services/storage.js`

```javascript
import { uploadData, getUrl, remove } from 'aws-amplify/storage';

export async function uploadReceipt(file, assetId) {
  try {
    const fileExtension = file.name.split('.').pop();
    const fileName = `receipts/${assetId}-${Date.now()}.${fileExtension}`;

    const result = await uploadData({
      path: fileName,
      data: file,
      options: {
        contentType: file.type,
      }
    }).result;

    // Get public URL
    const url = await getUrl({ path: fileName });
    return url.url.toString();
  } catch (error) {
    console.error('Error uploading file:', error);
    throw error;
  }
}

export async function deleteReceipt(fileKey) {
  try {
    await remove({ path: fileKey });
  } catch (error) {
    console.error('Error deleting file:', error);
    throw error;
  }
}

// Usage in component:
// const receiptUrl = await uploadReceipt(selectedFile, assetId);
```

---

## 6. Update App.jsx with Auth

### File: `src/App.jsx` (Add auth wrapper)

```javascript
import React, { useState, useEffect } from 'react';
import { getCurrentUser } from 'aws-amplify/auth';
import { LoginForm } from './components/Auth/LoginForm';
import { SignUpForm } from './components/Auth/SignUpForm';
import './aws-config'; // Import at the top

function AuthWrapper({ children }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);
  const [showSignUp, setShowSignUp] = useState(false);

  useEffect(() => {
    checkUser();
  }, []);

  async function checkUser() {
    try {
      const currentUser = await getCurrentUser();
      setUser(currentUser);
    } catch (err) {
      setUser(null);
    } finally {
      setLoading(false);
    }
  }

  if (loading) {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <div className="text-2xl font-bold">Loading...</div>
      </div>
    );
  }

  if (!user) {
    return (
      <div className="min-h-screen flex items-center justify-center" style={{ backgroundColor: '#FEF5E8' }}>
        {showSignUp ? (
          <>
            <SignUpForm onSuccess={() => checkUser()} />
            <button
              onClick={() => setShowSignUp(false)}
              className="absolute top-4 right-4 text-sm font-semibold"
            >
              Already have an account? Sign In
            </button>
          </>
        ) : (
          <>
            <LoginForm onSuccess={() => checkUser()} />
            <button
              onClick={() => setShowSignUp(true)}
              className="absolute top-4 right-4 text-sm font-semibold"
            >
              New user? Sign Up
            </button>
          </>
        )}
      </div>
    );
  }

  return children;
}

// Wrap your existing App component:
function App() {
  return (
    <AuthWrapper>
      {/* Your existing Router and routes */}
    </AuthWrapper>
  );
}
```

---

## 7. Environment Variables

### File: `.env.local` (Create this)

```env
VITE_AWS_REGION=us-east-1
VITE_AWS_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_AWS_USER_POOL_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_AWS_API_ENDPOINT=https://XXXXXXXXXXXX.appsync-api.us-east-1.amazonaws.com/graphql
VITE_AWS_S3_BUCKET=abjad-receipts-images-XXXXX
```

### Update `vite.config.js`:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/Abjad/',
  define: {
    'process.env': {}
  }
})
```

---

## 8. Testing Locally Before AWS

```bash
# Install Amplify mocks
npm install -g @aws-amplify/cli-mock

# Start local mock API
amplify mock api

# Start your app
npm run dev
```

---

## Next Steps

1. Run `amplify init` to create AWS resources
2. Copy generated values from `aws-exports.js`
3. Test authentication flow
4. Test API calls
5. Test file uploads
6. Deploy with `amplify publish`

Your app is now production-ready with AWS! 🚀
